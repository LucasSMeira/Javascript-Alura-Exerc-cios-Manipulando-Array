const array = [1,2,3];
const arrayDois = [4,5,6];


const concatenar = (array,arrayDois) =>{
    const arrayConcat = array.concat(arrayDois);
    return arrayConcat;
}

console.log(concatenar(array,arrayDois));